# Database Configuration
# SQL Server configuration
# Set your production connection string here
# Example:
# DATABASE_URL=mssql+pyodbc://USERNAME:PASSWORD@SERVER_HOST:PORT/DATABASE_NAME?driver=ODBC+Driver+18+for+SQL+Server&TrustServerCertificate=yes
DATABASE_URL=

# Monitor Configuration
CHECK_INTERVAL_SECONDS=120
INACTIVITY_THRESHOLD_MINUTES=30
ALERT_COOLDOWN_MINUTES=10

# Check Mode: "stored_procedure" only
CHECK_MODE=stored_procedure

# Stored Procedure Configuration (Combined SP returning both columns)
# The procedure must return a single row with these columns
SP_PROCEDURES=dbo.usp_CheckUpdateStatus:HotelUpdateStatus|ChannelStatsStatus

# Expected OK value for all procedures
SP_OK_VALUE=OK

# Teams Configuration (Optional - fallback)
TEAMS_WEBHOOK_URL=YOUR_TEAMS_WEBHOOK_URL_HERE
ENABLE_TEAMS_NOTIFICATIONS=true
ENABLE_EMAIL_NOTIFICATIONS=true

# SMTP Configuration (Primary notification method)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=adityak3521@gmail.com
SMTP_PASSWORD=krutlnjkalgyeqnc
SMTP_USE_TLS=false
SMTP_USE_SSL=true

# Email Configuration (Primary notification method)
MAIL_SENDER=adityak3521@gmail.com
MAIL_RECIPIENTS=aditya.kumar@rategain.com

# App Configuration
APP_NAME=db-activity-monitor
